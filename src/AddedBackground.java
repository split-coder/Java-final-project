import processing.core.PImage;

import java.util.List;

    class AddedBackground extends Background {

        public AddedBackground(String id, List<PImage> images) {
            super(id, images);
        }
    }

